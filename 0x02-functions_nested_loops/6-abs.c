#include "holberton.h"

/**
 * _abs - prints the absolute valor of a number
 *@n: is a parameter
 * Return: n if n is positive, -n if n is negative
 */
int _abs(int n);
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}

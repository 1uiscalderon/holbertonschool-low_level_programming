#include "holberton.h"

/**
 * _isalpha - returns 1 if a letter is lower or upper case
 *@c: is a parameter
 * Return: 1 if c is lowercase, 0 if it isnt
 */
int add(int, int)
{
	int sum;

	sum = a + b;
	return (c);
}
